name: Setup pnpm
description: Configures pnpm and installs dependencies

runs:
  using: composite
  steps:
    - name: Setup pnpm
      uses: pnpm/action-setup@v2.2.2
      with:
        version: 7.5.0

    - name: Install dependencies
      run: pnpm install
      shell: bash
